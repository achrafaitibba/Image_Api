spring:
  datasource:
    url: jdbc:mysql://localhost:3306/image
    jdbc-url: jdbc:mysql://localhost:3306/img
    username: root
    password: achraf
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
